# Prompt 06 — Implementation Plan

Create a bite-sized implementation plan for the approved NHSCopilot-Eval spec.

Sequence work so the project stays runnable after every step. List exact files, schemas, interfaces, tests, command, expected output, size, dependencies, evidence, rights decisions, and approval gates for every step. Put provenance, strict row contracts, public/private/sealed split, local baseline, redaction, replay, and disclosure checks before UI polish or provider adapters. Do not use vague placeholders.
